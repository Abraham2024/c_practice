#include <stdio.h>

/**
 * main - main entry function
 * description - prints the value of a constant
 * @j - value to be printed
 * return - 0
 */
int main(void)
{
	const int j = 10;
		const float PI = 3.14;
		printf("%i\n", j);
		printf("%.2f\n", PI);
		return(0);
}
