#include <stdio.h>

/**
 * main - main entry  point
 * description - prints the value  of j
 * return - 0
 */
int main(void)
{
	int i = 19;
		int  j = 21;
		i = j;
		printf(" the answer is %d\n", j);
		j = i;
			printf("the answer is still %d\n", i);
		return(0);
}
