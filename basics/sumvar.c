#include <stdio.h>

/**
 * main - main entry point
 * description - prints the sum of the variables
 * return - 0
 */
int main(void)
{
	int x = 9;
		float y = 5.6;
		float sum = x + y;
		printf("the answer is %f\n", sum);
		return(0);
}
